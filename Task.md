# 聲音刺激與腦波（EEG）實驗專案

## 一、專案目標

本專案直接以 Python 程式產生可控制的雙耳聲音刺激，並實作混音、空間化、節奏、噪音、提示音、即時播放、WAV 匯出及自動化測試。實驗期間同步記錄 EEG 與受試者主觀量表，用來比較不同聲音參數對腦波、注意力、放鬆程度與睡意的影響。

本專案的核心不是直接宣稱某個頻率具有固定功效，而是在相同設備、音量、時間與環境下，逐一控制變量並驗證效果。所有「專心、冥想、喚醒、放鬆、去除腦霧、修復、熟睡」等名稱，在實驗中只作為聲音條件標籤或待驗證假設，不視為醫療效果。

## 二、程式架構與技術選擇

Python 是唯一的聲音運算核心，避免研究條件在不同音訊引擎中產生不一致的結果。系統分成六個部分：

1. **聲音產生器**：使用 NumPy 建立時間軸、波形、差頻、包絡、琶音、節奏提示音與噪音。
2. **訊號處理器**：使用 SciPy 實作濾波、頻率響應、FFT、空間化所需的卷積與重取樣。
3. **混音與輸出器**：混合各聲音層，執行淡入淡出、峰值保護與音量正規化，再由 SoundFile 匯出 WAV；即時播放使用 `sounddevice` 或 PyAudio。
4. **音訊驗證器**：使用 NumPy、SciPy 與 Librosa 分析輸出檔的頻率、拍頻、BPM、頻譜、聲道及響度，確認實際結果符合設定。
5. **實驗控制器**：讀取 YAML 或 JSON 設定，排列實驗段落、固定亂數種子、寫入事件標記，並保存每次實驗的完整參數。
6. **EEG 與行為紀錄器**：接收 EEG 資料與時間標記，同步保存主觀量表、反應時間或指定認知任務結果。

所有可調參數都必須能被記錄與重現。每次輸出需保存亂數種子、聲音條件、開始與結束時間、淡入淡出時間、實際音量及 EEG 標記編號。

### 2.1 Python 套件

必要套件：

- **NumPy**：產生立體聲取樣陣列、波形、包絡、聲像、混音與亂數序列。
- **SciPy**：濾波、FFT、頻譜峰值偵測、卷積、重取樣及訊號分析。
- **SoundFile**：以浮點或 PCM 格式讀寫 WAV，保存測試與正式實驗輸出。
- **Librosa**：驗證 BPM、頻譜與音訊特徵；不放在即時聲音產生的必要路徑中。
- **pytest**：執行單元測試、參數化測試與整合測試。

播放套件優先採 `sounddevice`，因為可直接播放 NumPy 陣列；若硬體或作業系統相容性需要，再使用 PyAudio。播放層必須可替換，離線產生與自動測試不應依賴實體音效卡。

### 2.2 可選網頁介面

如需瀏覽器介面，可使用 Tone.js 製作參數面板、播放控制、波形／頻譜顯示與實驗狀態頁面。正式實驗的聲音仍由 Python 預先渲染或由 Python 服務提供，網頁不得自行重新解釋實驗公式。若未來選擇完全在瀏覽器播放，需另外建立與 Python 輸出的逐項一致性測試。

### 2.3 建議程式目錄

```text
double_ear/
├─ src/
│  └─ double_ear/
│     ├─ oscillators.py      # 正弦、方波、脈衝與包絡
│     ├─ binaural.py         # 雙耳差頻及差頻擺盪
│     ├─ rhythm.py           # BPM、錯拍與提示音
│     ├─ noise.py            # 白、棕、綠噪音
│     ├─ arpeggio.py         # 三種隨機琶音
│     ├─ spatial.py          # 聲像、距離及左右移動
│     ├─ mixer.py            # 多軌混音與輸出保護
│     ├─ render.py           # 設定檔轉 WAV
│     ├─ playback.py         # 即時播放
│     ├─ analysis.py         # FFT、BPM 與輸出驗證
│     └─ experiment.py       # 條件排列、標記與資料紀錄
├─ configs/                  # YAML／JSON 實驗條件
├─ tests/                    # pytest 自動測試
├─ outputs/                  # 產生的 WAV 與中繼資料
├─ pyproject.toml
└─ README.md
```

所有聲音函式以「參數輸入、NumPy 立體聲陣列輸出」為基本介面。運算預設使用 `float64`，匯出或播放前再轉為需要的格式。取樣率預設為 `48,000 Hz`，並由設定檔覆寫。

## 三、聲音引擎規格

### 3.1 雙耳搏動

左右耳分別播放略有差異的純音。設基礎頻率為 `f`、目標差頻為 `d`，預設採對稱配置：

- 左耳：`f - d / 2`
- 右耳：`f + d / 2`

待測差頻為：`40、18、14、7.83、4、3、2.5 Hz`。

待測基礎頻率為：

- `200 Hz`：一般基準條件
- `136.1 Hz`：冥想條件標籤
- `400–480 Hz`：喚醒條件標籤，可用固定頻率或緩慢掃頻
- `852 Hz`：清晰／去腦霧條件標籤
- `432 Hz`：修復條件標籤

每個音色需支援正弦波、方波與脈衝波，但雙耳差頻的第一輪比較以正弦波為主，避免高次諧波成為額外變量。方波用於後續清醒度實驗；脈衝波主要用於提示音，不作為長時間載波的預設波形。

所有條件必須提供「零差頻」控制：左右耳播放相同頻率，但總能量、音色與播放時間保持一致。

### 3.2 差頻擺盪

讓雙耳差頻以 4 秒為完整週期，在目標差頻附近平滑上下擺動。使用正弦低頻振盪器控制，避免頻率突變產生點擊聲。

擺幅依中心差頻分級：

- `2.5–4 Hz`：建議擺幅 `±1 Hz`
- `7.83–18 Hz`：建議擺幅 `±2 Hz`
- `40 Hz`：建議擺幅 `±3 Hz`

程式需限制最低瞬時差頻大於 `0 Hz`。實驗比較固定差頻與擺盪差頻，其他參數保持相同，以判斷動態變化本身是否造成差異。

### 3.3 多基頻疊加

在相同目標差頻下，同時疊加多組基礎頻率。每一組都維持相同左右差頻，但需避免所有聲部同相疊加造成峰值過高。

首輪設計使用 `100–200 Hz` 範圍內的 2 至 4 個載波；各聲部採獨立音量與輕微相位偏移，總輸出先做等響度校正，再與單一基頻條件比較。需記錄載波數量、各載波頻率、各自增益與總 RMS／LUFS。

主要比較：

- 單一基頻對多基頻
- 2、3、4 層疊加的層數效應
- 固定音程配置對緩慢變化配置

### 3.4 空間角度與距離感

空間化模組需獨立控制水平角度、前後感與距離感。耳機實驗優先使用雙耳聲像或 HRTF；若尚未導入 HRTF，可先以聲像、音量差、高頻衰減、早期反射與殘響比例建立簡化版本。

可調參數包括：

- 水平角度：左 `-90°` 至右 `+90°`
- 前後位置：前方、中央、後方三段，或連續值
- 距離：近、中、遠
- 移動速度與移動曲線
- 乾聲／殘響比例

第一輪只測固定位置；第二輪再測前後移動與左右移動。空間化處理不得破壞左右耳原定的瞬時頻差，且各位置需要做感知音量校正。

### 3.5 高頻隨機琶音

琶音作為獨立聲音層，模擬植物電磁訊號式的短促電子音。預設使用短包絡、柔和濾波與受限亂數，避免完全隨機導致條件無法重現。

首版設計八種可直接切換的風格：

1. **晨露點光**：不規則出現單音，音與音之間保留較長空白。
2. **藤蔓上升**：依音階逐步攀升再回落，形成連續生長感。
3. **孢子群聚**：短時間出現密集音群，群組之間保留較長間隔。
4. **晶體折射**：以五度與八度大幅跳躍，形成清晰明亮的幾何感。
5. **螢火漂移**：短音在左右空間隨機閃現，聲像範圍較寬。
6. **數位細雨**：高密度、短音長的下降序列，使用較低的單音音量。
7. **星圖序列**：固定音符骨架配合少量變奏，兼具規律性與生成感。
8. **花粉布朗運動**：音高只向相鄰步階移動，形成連續但不可預測的游走。

每一型均需控制音域、音階集合、事件密度、音長、音量、立體聲位置與亂數種子。琶音音量應顯著低於主要載波，並安排「無琶音」控制條件。

### 3.6 空間移動的搏動

此模組在保留雙耳頻差的同時，週期性互換左右耳的聲音角色，或讓兩個聲源沿左右空間軌跡交叉移動。移動頻率可在每秒 `4–7 次` 間調整。

為避免硬切換產生爆音，左右互換必須使用等功率交叉淡化。可控制項目包括：

- 交換／移動頻率：`4、5、6、7 Hz`
- 交叉淡化比例與曲線
- 移動深度：局部偏移至完全左右互換
- 移動相位與雙耳差頻相位的關係

此模組應與一般固定雙耳搏動分開比較，確認效果來自空間移動、差頻，或兩者交互作用。

### 3.7 錯拍提示音

以短促高頻點音建立左右耳不同重音位置，研究雙耳複節奏與注意力同步。節奏比例包括：`3:4、3:7、4:7、5:7`。實作時以同一小節長度為共同週期，左耳與右耳各自在該週期內平均放置指定數量的提示音。

需控制：

- 左右耳節拍數與指定聲道
- 提示音頻率、音長與包絡
- 重音強度
- 小節長度與 BPM
- 兩側節奏的起始相位

控制條件包括左右同拍，以及僅播放單耳節奏。提示音需使用短淡入淡出，避免波形不連續產生非預期寬頻聲響。

### 3.8 噪音

噪音作為背景聲層，分為：

- **棕噪音**：ADHD／專注條件標籤
- **綠噪音**：放鬆條件標籤；實作時必須明確定義頻譜範圍與濾波曲線
- **白噪音**：聲音遮蔽／聲牆條件標籤

三種噪音需以相同感知音量比較，並設置無噪音控制。噪音產生器需避免 DC 偏移，輸出前加入高通、限幅與安全增益。綠噪音沒有唯一工程標準，因此專案中採用的中心頻段與濾波規格必須固定並寫入紀錄。

### 3.9 BPM 與節奏用途

預設 BPM 條件為：

- `60 BPM`：熟睡條件標籤
- `80 BPM`：閱讀條件標籤
- `120 BPM`：專注條件標籤
- `128 BPM`：舞動／喚醒條件標籤

BPM 只控制節奏事件，不直接等同雙耳差頻。若同一實驗同時包含 BPM、錯拍與雙耳差頻，三者必須使用同一主時鐘並各自留下事件標記。

## 四、混音與輸出要求

混音器至少包含以下通道：主載波、第二載波群、噪音、琶音、提示音與空間效果回傳。每個通道需有靜音、獨奏、增益、左右聲像、濾波與效果發送控制。

總輸出必須具備：

- 5 至 10 秒淡入與淡出
- DC 移除與低頻保護
- 峰值限制器，避免突然爆音
- 左右聲道峰值與 RMS 監看
- 可校正的總音量上限
- 錄製實際輸出或離線渲染的功能

切換條件時不得瞬間改變頻率、音量或空間位置。所有轉換使用平滑參數或交叉淡化，並在 EEG 資料中標記轉換區間，分析時排除該段。

## 五、實驗設計原則

本專案變量很多，不應一次測試所有組合。採分階段、受試者內設計：同一位受試者體驗多個條件，但條件順序隨機或以平衡順序排列。每一階段只改變一個主要因素；通過初步篩選後，才測試兩個因素的交互作用。

共同控制條件如下：

- 相同耳機、音訊介面、取樣率與環境
- 相同播放音量與校正流程
- 相同實驗時長、姿勢、睜眼／閉眼狀態與任務
- 記錄睡眠、咖啡因、藥物、時間帶及測試時段
- 每段前保留基線，段落間安排安靜休息
- 條件名稱對受試者盡量中性，降低期待效應
- 操作者與分析檔案使用編碼條件，降低分析偏差

建議每個條件包含：`2–5 分鐘基線 + 8–15 分鐘刺激 + 2–5 分鐘恢復`。正式時間需先用小規模測試確認受試者可接受度與 EEG 訊號品質。

## 六、分階段實驗項目

### 實驗 0：設備、音量與訊號驗證

目的為確認左右聲道、頻率精度、事件標記延遲、EEG 同步及安全音量。測試純音、零差頻、已知差頻、淡入淡出與左右互換，並以錄音或分析工具確認實際輸出頻譜。此階段不判斷心理效果。

### 實驗 1：雙耳差頻篩選

固定 `200 Hz` 正弦基頻，比較 `40、18、14、7.83、4、3、2.5 Hz` 與零差頻控制。主要觀察刺激期間相對基線的 EEG 頻帶功率、左右連結性、主觀專注／放鬆／睡意，以及認知任務表現。

此階段的結果用來選出少量候選差頻，避免後續組合爆炸。

### 實驗 2：基礎頻率與波形

使用實驗 1 選出的差頻，分別測試 `136.1、200、432、400–480、852 Hz`。先固定正弦波比較基頻，再對少量候選條件比較正弦波與方波。脈衝波只在短提示音實驗中測試。

主要問題是：觀察到的差異是否由差頻造成，還是由載波音高、音色與可聽舒適度造成。

### 實驗 3：固定差頻與差頻擺盪

以同一中心差頻比較固定條件與 4 秒週期擺盪條件。依中心差頻套用 `±1、±2 或 ±3 Hz` 擺幅。記錄擺盪週期相位，以便分析 EEG 是否跟隨瞬時差頻變化。

### 實驗 4：單基頻與多基頻疊加

比較單一載波、2 層、3 層及 4 層載波。保持目標差頻、播放時間與感知音量相同。除 EEG 外，需特別記錄刺耳度、疲勞感、音色喜好與是否出現明顯拍頻干擾。

### 實驗 5：固定空間位置與空間移動

第一部分比較前、中央、後與左右固定位置；第二部分比較靜止、緩慢移動及每秒 `4–7 次` 的左右交換。所有條件維持相同差頻與總音量，事件標記需包含每次通過中央及左右互換時刻。

### 實驗 6：雙耳複節奏

以高頻點音比較同拍控制與 `3:4、3:7、4:7、5:7` 錯拍。BPM 固定後再比較比例，避免 BPM 與節奏比例同時改變。主要觀察節奏同步相關 EEG、注意力任務表現、主觀規律感與疲勞度。

### 實驗 7：噪音、BPM 與琶音聲層

分成三個獨立子實驗：

- 無噪音、棕噪音、綠噪音與白噪音比較
- `60、80、120、128 BPM` 比較
- 無琶音、稀疏點狀、生長序列與群聚脈衝比較

三個子實驗不可在首輪全部交叉組合。先各自篩選，再挑選少量條件進入整合測試。

### 實驗 8：雙耳複節奏與差頻的交互作用

使用前述實驗選出的 1 至 2 個差頻與 1 至 2 個錯拍比例，採二因子設計：有／無差頻 × 同拍／錯拍。判斷兩種刺激是相加、互相干擾，還是只有其中一種主導反應。

### 實驗 9：空間移動與雙耳差頻的交互作用

重點測試差頻 `3 Hz` 與空間移動 `4 Hz` 等不同速率同時存在時的反應。採二因子設計：

- 固定空間、零差頻
- 固定空間、指定差頻
- 指定空間移動、零差頻
- 指定空間移動、指定差頻

再比較移動頻率與差頻之間的比例關係，例如相同、接近及明顯不同的速率。分析時同時觀察各自頻率、和頻／差頻附近的 EEG 反應，以及主觀不適、分心與空間錯覺。

## 七、EEG、行為與主觀指標

每個實驗需在開始前定義主要指標，避免看完資料後才挑選結果。建議紀錄：

- EEG：頻帶功率、刺激頻率附近反應、左右腦差異、跨區連結性與事件相關變化
- 行為：反應時間、正確率、漏答率；任務依專注、閱讀或喚醒目標固定選擇
- 主觀量表：專注、放鬆、睡意、清晰度、愉悅度、疲勞、不適與暈眩
- 聲音品質：刺耳度、音量感、空間感與節奏可察覺度

EEG 前處理需保留原始資料，另存濾波、重參考、眼動／肌電偽影處理及排除片段的紀錄。分析人員應先確認刺激產生的電磁或耳機線材干擾不會被誤判為腦波同步。

## 八、資料與設定格式

每次實驗至少保存下列欄位：

`participant_id、session_id、condition_id、carrier_hz、beat_hz、beat_swing_hz、swing_period_s、waveform、noise_type、noise_level、bpm、polyrhythm_ratio、arpeggio_type、spatial_mode、movement_hz、duration_s、sample_rate、output_level、random_seed、marker_times`。

條件設定應由 JSON、YAML 或 CSV 載入，避免手動改程式造成版本差異。程式碼、設定檔、輸出音檔與分析結果均需標示版本。

每次產生音訊時，同時輸出同名的中繼資料檔，內容包含完整設定、程式版本、產生時間、音訊檔雜湊值及自動驗證結果。相同設定與亂數種子必須能產生相同的離線音訊。

## 九、程式測試與音訊驗證

測試分為單元測試、訊號測試、整合測試及人工聆聽測試。正式實驗只能使用全部自動測試通過並完成聆聽確認的程式版本。

### 9.1 單元測試

使用 pytest 對每一個聲音模組進行參數化測試：

- 輸出形狀必須為 `(取樣數, 2)`，且左右聲道順序固定。
- 音訊不得包含 `NaN`、無限值或非預期 DC 偏移。
- 峰值不得超出允許範圍，限制器不得造成大量削波。
- 同一亂數種子必須重現完全相同的噪音與琶音事件。
- 不合法參數必須明確拒絕，例如負頻率、差頻大於可用範圍、零秒長度或無效波形名稱。
- 混音、淡入淡出及交叉淡化的長度和端點必須正確。

### 9.2 訊號正確性測試

使用 SciPy FFT 或頻譜估計驗證實際輸出：

- 左右載波的頻譜峰值需符合 `f - d/2` 與 `f + d/2`，誤差容許值由 FFT 解析度決定。
- 零差頻條件的左右主要頻率必須相同。
- 差頻擺盪需在設定週期及上下界內變化。
- 方波諧波、正弦波純度及脈衝包絡需符合各自定義。
- 棕、綠、白噪音需以固定頻譜斜率或頻段能量規則驗證。
- 錯拍事件數量、時間點及共同小節長度必須符合 `3:4、3:7、4:7、5:7`。
- `60、80、120、128 BPM` 的事件間隔必須通過程式計算，並以 Librosa 作第二層檢查。
- 空間互換頻率及交叉淡化週期需符合 `4–7 Hz`，切換點不得產生明顯不連續。

### 9.3 整合與回歸測試

整合測試從設定檔建立完整音訊，匯出 WAV，再重新讀入分析。驗證取樣率、聲道數、時長、頻率、峰值、中繼資料及檔案雜湊。建立一組短版標準條件作為回歸測試，任何程式修改後都重新產生並比較關鍵特徵。

測試時使用 1 至 10 秒短音檔以縮短執行時間；正式輸出才使用完整實驗長度。自動測試不得直接播放聲音，以免測試過程突然從喇叭發聲。

### 9.4 人工聆聽與硬體測試

自動測試通過後，才進行低音量人工檢查。確認左右聲道、爆音、底噪、空間移動方向與主觀音量一致性。硬體測試需記錄耳機、音訊介面、驅動、取樣率與輸出音量位置，並以測試音確認系統沒有額外的音效增強或空間處理。

### 9.5 執行入口

程式至少提供以下命令用途：

```text
python -m double_ear.render --config configs/example.yaml --output outputs/example.wav
python -m double_ear.analysis --audio outputs/example.wav --config configs/example.yaml
python -m double_ear.playback --audio outputs/example.wav
pytest
```

`render` 只負責確定性產生音檔，`analysis` 負責檢查音檔是否符合設定，`playback` 必須要求明確指定檔案後才播放。測試結果應能在終端機及機器可讀報告中查看。

## 十、安全與倫理

- 正式實驗前須完成研究倫理、知情同意與退出機制。
- 使用經校正且舒適的音量；開始前先做短時間試聽。
- 受試者可隨時停止；出現耳鳴、耳痛、頭痛、暈眩、噁心或明顯焦慮時立即中止。
- 不以本系統取代醫療診斷或治療，也不向受試者承諾特定療效。
- 對聽力異常、神經系統疾病、光／聲敏感或其他風險族群，須由研究倫理與專業人員決定納入標準。
- EEG 與個人資料使用匿名識別碼保存，分離存放同意書與研究資料。

## 十一、專案完成標準

1. Python 可依設定檔獨立產生全部基礎聲音模組，不依賴其他音訊合成環境。
2. 每個模組都有 pytest 單元測試及訊號正確性測試。
3. 程式可輸出標準立體聲 WAV，並能透過獨立播放模組安全播放。
4. 分析程式能自動驗證頻率、BPM、聲道、時長、頻譜與峰值。
5. 混音器可獨立控制各音層，且切換時無爆音、無非預期削波。
6. 每個實驗條件具有零差頻、無效果或同拍等適當控制。
7. 聲音輸出、EEG 與事件標記完成同步驗證。
8. 所有參數、亂數種子、程式版本與輸出音量皆可追溯。
9. 相同設定與亂數種子可產生相同的離線音訊結果。
10. 先完成單一變量實驗，再進行複節奏及空間移動的交互作用實驗。
11. 產出可重複執行的實驗設定、音訊、原始資料、測試報告、分析流程與結果報告。
